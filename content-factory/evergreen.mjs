// 에버그린 주제 큐 — 실시간 이슈가 아니어도 사람들이 계속 찾아보는 정보 콘텐츠.
//
// 【왜 필요한가】
//  · 뉴스는 하루 지나면 죽지만, 꿀팁·생활정보는 검색 유입이 계속 쌓인다.
//  · 실시간 수집이 필요 없어 AI 한도 부담이 적고, 미리 만들어둘 수 있다.
//  · 공식 문서 기반이라 가짜뉴스 위험이 거의 없다.
//
// 【가장 중요한 규칙】
//  주제마다 '검증된 사실(facts)'을 함께 적어둔다.
//  원문 기사가 없으므로, AI는 오직 이 facts 만 근거로 글을 쓴다. (지어내기 차단)
//  → facts 에 없는 내용은 쓰면 안 된다. 새 주제를 추가할 땐 반드시 사실을 확인하고 적을 것.
//
// 【제목 규칙】 "N가지" 형태면 facts 에도 N개가 정확히 들어있어야 한다.
//  (게시물 안에서 정보가 완결되어야 하므로)

// ─────────────────────────────────────────────────────────────
// 상시 주제 — 아무 때나 내도 되는 것
// ─────────────────────────────────────────────────────────────
export const EVERGREEN = [
  {
    id: "mac-shortcuts-7",
    category: "꿀팁",
    title: "맥 쓰면서 이건 몰랐지? 숨은 단축키 7가지",
    ref: { title: "Apple 지원 — Mac 키보드 단축키", url: "https://support.apple.com/ko-kr/HT201236" },
    facts: `맥(macOS) 기본 키보드 단축키 7가지. 모두 macOS에 기본 내장된 기능이다.

1) 화면 즉시 잠그기 — Command + Control + Q
   자리를 비울 때 누르면 바로 잠금 화면으로 넘어간다. 로그아웃이 아니라 잠금이라 하던 작업은 그대로 남는다.

2) 원하는 영역만 캡처 — Command + Shift + 4
   커서가 십자 모양으로 바뀌고, 드래그한 영역만 이미지로 저장된다. 전체 화면을 찍고 자를 필요가 없다.

3) 캡처 도구 전체 열기 — Command + Shift + 5
   화면 녹화, 창 단위 캡처, 저장 위치 지정까지 한 화면에서 고를 수 있다.

4) 스포트라이트 검색 — Command + Space
   앱 실행, 파일 찾기, 단위 변환, 계산까지 검색창 하나로 처리한다.

5) 이모지·기호 입력 — Control + Command + Space
   어떤 앱에서든 이모지와 특수문자 창을 띄운다.

6) 같은 앱의 창끼리 전환 — Command + \` (물결표 키)
   Command + Tab 은 앱 사이 이동이고, 이건 같은 앱에서 열어둔 창들 사이를 오간다.

7) Dock 숨기기/보이기 — Option + Command + D
   화면을 넓게 쓰고 싶을 때 Dock을 접었다 폈다 한다.`,
  },
  {
    id: "windows-shortcuts-7",
    category: "꿀팁",
    title: "윈도우 쓰면서 모르면 손해인 단축키 7가지",
    ref: { title: "Microsoft 지원 — Windows 키보드 단축키", url: "https://support.microsoft.com/ko-kr/windows/windows-키보드-단축키-dcc61a57-8ff0-cffe-9796-cb9706c75eec" },
    facts: `윈도우(Windows 10/11) 기본 키보드 단축키 7가지. 모두 윈도우에 기본 내장된 기능이다.

1) 복사 기록 다시 꺼내기 — Windows 키 + V
   최근 복사한 항목들이 목록으로 뜬다. 바로 전 것만이 아니라 여러 개를 골라 붙여넣을 수 있다. (처음 쓸 때 켜기 한 번 필요)

2) 원하는 영역만 캡처 — Windows 키 + Shift + S
   화면이 어두워지고 드래그한 부분만 잘라 복사된다.

3) 화면 즉시 잠그기 — Windows 키 + L
   자리를 비울 때 한 번에 잠금 화면으로 간다.

4) 창을 화면 반쪽에 착 붙이기 — Windows 키 + 방향키
   왼쪽/오른쪽으로 창을 정렬해 두 개를 나란히 볼 수 있다.

5) 바탕화면 한 번에 보기 — Windows 키 + D
   열어둔 창을 전부 내렸다가, 다시 누르면 그대로 복원된다.

6) 파일 탐색기 바로 열기 — Windows 키 + E
   시작 메뉴를 거치지 않고 바로 탐색기가 뜬다.

7) 이모지 입력 — Windows 키 + 마침표(.)
   어떤 입력창에서든 이모지 창을 띄운다.`,
  },
  {
    id: "phone-storage-5",
    category: "꿀팁",
    title: "폰 저장공간 부족할 때, 사진 안 지우고 비우는 법 5가지",
    ref: { title: "오즈백 정리 — 기기 저장공간 관리", url: "https://oddsbag.co.kr" },
    facts: `스마트폰 저장공간을 확보하는 방법 5가지. 사진·영상을 지우지 않고 용량을 줄이는 데 초점을 둔다.

1) 앱 캐시 비우기
   앱이 임시로 저장해둔 데이터다. 지워도 계정이나 저장물은 사라지지 않고, 앱을 다시 쓰면 필요한 만큼 다시 쌓인다.

2) 최근 삭제(휴지통) 폴더 비우기
   사진을 지워도 보통 30일간 휴지통에 남아 용량을 그대로 차지한다. 여기까지 비워야 실제로 공간이 생긴다.

3) 다운로드 폴더 정리
   메신저나 브라우저로 받은 파일이 계속 쌓인다. 대부분 다시 안 여는 파일들이다.

4) 안 쓰는 앱 정리
   설치만 해두고 안 쓰는 앱은 앱 용량 + 데이터까지 함께 차지한다.

5) 메신저에 쌓인 사진·영상 정리
   대화방에 받은 미디어가 기기 저장공간을 크게 먹는 경우가 많다. 대화 내용은 두고 미디어만 정리할 수 있다.`,
  },
];

// ─────────────────────────────────────────────────────────────
// 시즌 주제 — 정해진 시기 '전에' 미리 알려주는 것
//   month/day = 기준일, leadDays = 며칠 전부터 낼지
//   ※ 해마다 날짜가 조금씩 바뀌므로 facts 에는 '연도별 확정 날짜'를 쓰지 않는다.
// ─────────────────────────────────────────────────────────────
export const SEASONAL = [
  {
    id: "yearend-tax",
    category: "꿀팁",
    month: 1,
    day: 15,
    leadDays: 20,
    title: "연말정산, 미리 알아두면 덜 토해내는 것들",
    ref: { title: "국세청 홈택스", url: "https://www.hometax.go.kr" },
    facts: `연말정산 준비에 관한 일반적인 사실. 구체적 금액·비율은 해마다 바뀌므로 단정하지 않는다.

1) 간소화 서비스는 매년 1월 중순에 열린다
   국세청 홈택스에서 대부분의 공제 자료를 한 번에 내려받을 수 있다.

2) 간소화 자료에 안 잡히는 항목이 있다
   기부금, 안경 구입비, 일부 의료비, 월세 등은 직접 영수증을 챙겨야 하는 경우가 있다.

3) 부양가족 공제는 조건 확인이 먼저다
   소득·나이 요건이 있고, 형제자매가 중복으로 올리면 나중에 문제가 된다.

4) 월세액 공제는 조건이 맞아야 한다
   임대차계약서와 이체 내역 등 증빙이 필요하다.

5) 놓쳤어도 나중에 바로잡을 수 있다
   빠뜨린 공제는 이후 경정청구 절차로 다시 신청할 수 있다.`,
  },
  {
    id: "car-tax-prepay",
    category: "꿀팁",
    month: 1,
    day: 16,
    leadDays: 14,
    title: "자동차세 연납, 1월에 한 번에 내면 깎아준다",
    ref: { title: "위택스", url: "https://www.wetax.go.kr" },
    facts: `자동차세 연납(1년치 미리 납부) 제도에 관한 사실. 공제율은 해마다 조정될 수 있으므로 구체 수치는 단정하지 않는다.

1) 1년치를 미리 내면 일정 비율을 깎아준다
   원래는 6월과 12월에 나눠 내는데, 1월에 한 번에 내면 할인이 적용된다.

2) 신청은 위택스나 관할 지자체에서 한다
   온라인으로도 신청·납부가 가능하다.

3) 1월을 놓쳐도 기회가 남아 있다
   3월, 6월, 9월에도 남은 기간에 대해 연납을 신청할 수 있고, 늦어질수록 할인 폭은 줄어든다.

4) 차를 중간에 팔면 정산된다
   연납 후 차량을 처분하면 남은 기간만큼 환급받는다.`,
  },
];

const key = (t) => t.replace(/\s+/g, "").slice(0, 30);

// 오늘 낼 만한 시즌 주제 (기준일 leadDays 전 ~ 기준일 사이)
function seasonalDue(now) {
  const y = now.getFullYear();
  return SEASONAL.filter((t) => {
    const target = new Date(y, t.month - 1, t.day);
    const diff = (target - now) / 86400000; // 남은 일수
    return diff <= t.leadDays && diff >= -1; // 기준일 직후 하루까지는 유효
  });
}

/**
 * 에버그린/시즌 주제를 이슈 형태로 돌려준다.
 * 뉴스가 안 잡히는 시간대에도 콘텐츠가 끊기지 않게 하는 안전망이기도 하다.
 *
 * @param {Set<string>} seen  이미 다룬 주제 키
 * @param {number} want       필요한 개수
 * @param {Date}   now
 */
export function pickEvergreenIssues(seen, want = 2, now = new Date()) {
  const pool = [...seasonalDue(now), ...EVERGREEN]; // 시즌 주제를 먼저
  const out = [];
  for (const t of pool) {
    if (out.length >= want) break;
    if (seen.has(key(t.title))) continue;
    out.push({
      source: "오즈백 정리",
      title: t.title,
      link: t.ref?.url ?? null,
      category: t.category,
      facts: t.facts, // ★ 이게 있으면 파이프라인이 원문 수집 대신 이걸 근거로 쓴다
      ref: t.ref,
    });
  }
  return out;
}
